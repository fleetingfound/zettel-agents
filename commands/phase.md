---
description: Develop a phased plan based on SPEC
agent: planner
---

Develop a plan for the implementation of `SPEC.md`.

The first phase should implement a working application which implements a minimal set of the features specified in `SPEC.md`.

Each subsequent phase should add functionality to the previous implementation. By the final phase, every aspect of `SPEC.md` should be implemented. Consider different approaches for modularizing the application in a way that facilitates testing.
